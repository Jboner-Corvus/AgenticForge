/// <reference types="vitest/globals" />
import { describe, it } from 'vitest';

describe('browserTool', () => {
  it('should have a test', () => {
    // This is a placeholder test to prevent vitest from complaining
  });
});
